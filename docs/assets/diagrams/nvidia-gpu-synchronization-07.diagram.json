{
  "kind": "flowchart",
  "claim": "Completion plus the required publication/visibility handoff establishes full(slot,g), but the producer may reuse the physical stage only after the consumer returns empty(slot,g+1).",
  "question": "Which ownership states prevent a producer from overwriting a tile that is still in use?",
  "audience": "CUDA developers implementing reusable shared-memory pipelines",
  "scope": "One physical slot advancing from generation g to g+1; excludes two-stage overlap timing and primitive-specific implementation details",
  "readingOrder": "Follow the clockwise cycle from empty(slot,g) through Filling, full(slot,g), and Consuming until the consumer returns empty(slot,g+1)",
  "evidence": ["docs/notes/nvidia-gpu-synchronization.md"],
  "localeScope": "zh-only",
  "presentation": "zoom",
  "scorecard": {"necessity":18,"technicalCorrectness":25,"readability":18,"hierarchy":14,"terminology":9,"accessibility":5,"maintainability":5,"total":94},
  "outcome": "pass"
}
