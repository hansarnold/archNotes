%%{init: {"themeVariables": {"fontSize": "22px"}, "flowchart": {"diagramPadding": 8, "nodeSpacing": 24, "rankSpacing": 28, "padding": 8}}}%%
flowchart LR
    subgraph LEFT[" "]
        direction TB
        F["FILLING<br/>producer owns write"]
        U["FULL<br/>consumer may acquire"]
    end
    subgraph RIGHT[" "]
        direction BT
        E["EMPTY<br/>producer may acquire"]
        C["CONSUMING<br/>consumer owns read"]
    end

    E -->|"acquire empty(slot,g)"| F
    F -->|"complete + publish"| U
    U -->|"acquire full(slot,g)"| C
    C -->|"return empty(slot,g+1)"| E

    %% Layout-only constraint; it does not add a state transition.
    RIGHT ~~~ LEFT
