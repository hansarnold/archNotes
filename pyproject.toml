[project]
name = "archnotes"
version = "0.4.0"
description = "Architecture notes and executable learning labs for GPU, Groq, Tenstorrent, and Google TPU accelerators"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
